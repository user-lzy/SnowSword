#VisualFreeBasic_Form#  Version=5.9.7
Locked=0

[Form]
Name=FrmListView
Help=False
ClassStyle=CS_VREDRAW,CS_HREDRAW,CS_DBLCLKS
ClassName=
WinStyle=WS_CAPTION,WS_SYSMENU,WS_MINIMIZEBOX,WS_MAXIMIZEBOX,WS_CLIPSIBLINGS,WS_CLIPCHILDREN,WS_VISIBLE,WS_EX_WINDOWEDGE,WS_EX_CONTROLPARENT,WS_EX_LEFT,WS_EX_LTRREADING,WS_EX_RIGHTSCROLLBAR,WS_POPUP,WS_SIZEBOX
Style=3 - 常规窗口
Icon=
Caption=
StartPosition=1 - 屏幕中心
WindowState=0 - 正常
Enabled=True
Repeat=True
Left=0
Top=0
Width=775
Height=538
TopMost=False
Child=False
MdiChild=False
TitleBar=True
SizeBox=True
SysMenu=True
MaximizeBox=True
MinimizeBox=True
Help=False
Hscroll=False
Vscroll=False
MinWidth=0
MinHeight=0
MaxWidth=0
MaxHeight=0
NoActivate=False
MousePass=False
TransPer=0
TransColor=SYS,25
Shadow=0 - 无阴影
BackColor=SYS,15
MousePointer=0 - 默认
Tag=
Tab=True
ToolTip=
ToolTipBalloon=False
AcceptFiles=False

[PopupMenu]
Name=mnuWebConnect
Help=
Index=-1
Menu=刷新FrmListView_mnuWebConnect_mnuRefresh0-10-FrmListView_mnuWebConnect_mnuStep10-10关闭连接FrmListView_mnuWebConnect_mnuCloseConnect0-10-FrmListView_mnuWebConnect_mnuStep20-10显示全部FrmListView_mnuWebConnect_mnuViewAll0-10-FrmListView_mnuWebConnect_mnuStep30-10复制FrmListView_mnuWebConnect_mnuCopy0-10{复制单格数据FrmListView_mnuWebConnect_mnuLittleCopy0-10}定位文件位置(资源管理器)FrmListView_mnuWebConnect_mnuLocateFilePath0-10
Left=680
Top=190
Tag=

[PopupMenu]
Name=mnuWindow
Help=
Index=-1
Menu=刷新FrmListView_mnuWindow_mnuRefresh0-10-FrmListView_mnuWindow_mnuStep10-10显示窗口FrmListView_mnuWindow_mnuShowWindow0-10隐藏窗口FrmListView_mnuWindow_mnuHideWindow0-10最小化窗口FrmListView_mnuWindow_mnuMinWindow0-10最大化窗口FrmListView_mnuWindow_mnuMaxWindow0-10-FrmListView_mnuWindow_mnuStep20-10摧毁窗口FrmListView_mnuWindow_mnnuDestroyWindow0-10冻结窗口FrmListView_mnuWindow_mnuFreezeWindow0-10激活窗口FrmListView_mnuWindow_mnuActiveWindow0-10窗口置顶FrmListView_mnuWindow_mnuWindowTop0-10取消置顶FrmListView_mnuWindow_mnuWindowNoTop0-10-FrmListView_mnuWindow_mnuStep30-10复制FrmListView_mnuWindow_mnuCopy0-10{复制单格数据FrmListView_mnuWindow_mnuLittleCopy0-10}
Left=610
Top=270
Tag=

[PopupMenu]
Name=mnuMemory
Help=
Index=-1
Menu=刷新FrmListView_mnuMemory_mnuRefresh0-10-FrmListView_mnuMemory_mnuStep10-10定位内存FrmListView_mnuMemory_mnuLocateMemoryCtrl+G0-10修改内存属性FrmListView_mnuMemory_mnuChangeMemory0-10查看/编辑内存FrmListView_mnuMemory_mnuEditMemory0-10复制FrmListView_mnuMemory_mnuCopy0-10{复制单格数据FrmListView_mnuMemory_mnuLittleCopy0-10}定位文件位置(资源管理器)FrmListView_mnuMemory_mnuLocateFilePath0-10
Left=550
Top=180
Tag=

[Label]
Name=lblNum
Help=
Index=-1
Style=0 - 无边框
Caption=
Enabled=True
Visible=True
ForeColor=SYS,8
BackColor=SYS,25
Font=微软雅黑,9,0
FontWidth=0
FontAngle=0
TextAlign=0 - 左对齐
Prefix=True
Ellipsis=False
Left=690
Top=490
Width=80
Height=20
Layout=0 - 不锚定
MousePointer=0 - 默认
Tag=
ToolTip=
ToolTipBalloon=False

[PopupMenu]
Name=mnuHandle
Help=
Index=-1
Menu=刷新FrmListView_mnuHandle_mnuRefresh0-10关闭FrmListView_mnuHandle_mnuCloseHandle0-10-FrmListView_mnuHandle_Step10-10复制FrmListView_mnuHandle_mnuCopy0-10{复制单格数据FrmListView_mnuHandle_mnuLittleCopy0-10}
Left=490
Top=260
Tag=

[TextBox]
Name=TxtFilter1
Help=
Index=-1
Style=3 - 凹边框
TextScrollBars=0 - 无滚动条
Text=
Enabled=True
Visible=True
MaxLength=0
ForeColor=SYS,8
BackColor=SYS,5
Font=微软雅黑,9,0
TextAlign=0 - 左对齐
PasswordChar=
Locked=False
HideSelection=True
Multiline=False
Uppercase=False
Lowercase=False
Number=False
AutoHScroll=True
AutoVScroll=False
Left=0
Top=490
Width=690
Height=20
Layout=0 - 不锚定
MousePointer=0 - 默认
Tag=
Tab=True
ToolTip=
ToolTipBalloon=False
LeftMargin=0
RightMargin=0
AcceptFiles=False

[PopupMenu]
Name=mnuUnlock_File
Help=
Index=-1
Menu=刷新FrmListView_mnuUnlock_FIle_mnuRefresh0-10关闭句柄FrmListView_mnuUnlock_FIle_mnuCloseHandle0-10强制关闭句柄FrmListView_mnuUnlock_FIle_mnuForceCloseHandle0-10结束进程FrmListView_mnuUnlock_FIle_mnuTerminateProcess0-10-FrmListView_mnuUnlock_File_mnuStep10-10复制FrmListView_mnuUnlock_File_mnuCopy0-10{复制单格数据FrmListView_mnuUnlock_File_mnuLittleCopy0-10}
Left=380
Top=250
Tag=

[PopupMenu]
Name=mnuModule
Help=
Index=-1
Menu=刷新FrmListView_mnuModule_mnuRefreshModule0-10卸载模块FrmListView_mnuModule_mnuUnloadModule0-10全局卸载模块FrmListView_mnuModule_mnuGlobalUnloadModule0-10强制卸载模块FrmListView_mnuModule_mnuForceUnloadModule0-10查看HookFrmListView_mnuModule_mnuViewHook0-10-FrmListView_mnuModule_mnuStep10-10查看/编辑内存FrmListView_mnuModule_mnuEditMemory0-10复制FrmListView_mnuModule_mnuCopy0-10{复制单格数据FrmListView_mnuModule_mnuLittleCopy0-10}定位文件位置(资源管理器)FrmListView_mnuModule_mnuLocateFilePath0-10
Left=450
Top=150
Tag=

[PopupMenu]
Name=mnuThread
Help=
Index=-1
Menu=刷新FrmListView_mnuThread_mnuRefreshThread0-10-FrmListView_mnuThread_Step10-10结束线程FrmListView_mnuThread_mnuTerminateThread0-10强制结束线程FrmListView_mnuThread_mnuForceKillThread0-10暴力摧毁线程(可能蓝屏)FrmListView_mnuThread_mnuForceDestroyThread0-10-FrmListView_mnuThread_Step20-10挂起(暂停)线程FrmListView_mnuThread_mnuSuspendThread0-10恢复线程FrmListView_mnuThread_mnuResumeThread0-10查看线程栈FrmListView_mnuThread_mnuViewThreadStack0-10-FrmListView_mnuThread_mnuStep30-10查看/编辑内存FrmListView_mnuThread_mnuEditMemory0-10复制FrmListView_mnuThread_mnuCopy0-10{复制单格数据FrmListView_mnuThread_mnuLittleCopy0-10}定位文件位置(资源管理器)FrmListView_mnuThread_mnuLocateFilePath0-10
Left=360
Top=150
Tag=

[PopupMenu]
Name=mnuPrivilege
Help=
Index=-1
Menu=刷新FrmListView_mnuPrivilege_mnuRefresh0-10-FrmListView_mnuPrivilege_mnuStep10-10启用特权FrmListView_mnuPrivilege_mnuEnablePrivilege0-10禁用特权FrmListView_mnuPrivilege_mnuDisablePrivilege0-10
Left=390
Top=350
Tag=

[PopupMenu]
Name=mnuMsgHook
Help=
Index=-1
Menu=刷新FrmListView_mnuMsgHook_mnuRefresh0-10-FrmListView_mnuMsgHook_mnuStep10-10移除FrmListView_mnuMsgHook_mnuRemove0-10-FrmListView_mnuMsgHook_mnuStep20-10显示全部FrmListView_mnuMsgHook_mnuViewAll0-10-FrmListView_mnuMsgHook_mnuStep30-10查看/编辑内存FrmListView_mnuMsgHook_mnuEditMemory0-10复制FrmListView_mnuMsgHook_mnuCopy0-10{复制整行数据FrmListView_mnuMsgHook_mnuCopyRowData0-10}
Left=500
Top=100
Tag=

[PopupMenu]
Name=mnuEventHook
Help=
Index=-1
Menu=刷新FrmListView_mnuEventHook_mnuRefresh0-10-FrmListView_mnuEventHook_mnuStep10-10移除FrmListView_mnuEventHook_mnuRemove0-10-FrmListView_mnuEventHook_mnuStep20-10显示全部FrmListView_mnuEventHook_mnuViewAll0-10-FrmListView_mnuEventHook_mnuStep30-10查看/编辑内存FrmListView_mnuEventHook_mnuEditMemory0-10复制FrmListView_mnuEventHook_mnuCopy0-10{复制整行数据FrmListView_mnuEventHook_mnuCopyRowData0-10}
Left=600
Top=100
Tag=

[PopupMenu]
Name=mnuHotkey
Help=
Index=-1
Menu=刷新FrmListView_mnuHotkey_mnuRefresh0-10-FrmListView_mnuHotkey_mnuStep10-10移除FrmListView_mnuHotkey_mnuRemove0-10-FrmListView_mnuHotkey_mnuStep20-10显示全部FrmListView_mnuHotkey_mnuViewAll0-10-FrmListView_mnuHotkey_mnuStep30-10查看/编辑内存FrmListView_mnuHotkey_mnuEditMemory0-10复制FrmListView_mnuHotkey_mnuCopy0-10{复制整行数据FrmListView_mnuHotkey_mnuCopyRowData0-10}
Left=660
Top=70
Tag=

[PopupMenu]
Name=mnuIOFunction
Help=
Index=-1
Menu=刷新FrmListView_mnuIOFunction_mnuRefresh0-10
Left=490
Top=360
Tag=

[PopupMenu]
Name=mnuProcessTimer
Help=
Index=-1
Menu=刷新FrmListView_mnuProcessTimer_mnuRefresh0-10-FrmListView_mnuProcessTimer_mnuStep10-10移除FrmListView_mnuProcessTimer_mnuRemove0-10-FrmListView_mnuProcessTimer_mnuStep20-10显示全部FrmListView_mnuProcessTimer_mnuViewAll0-10
Left=600
Top=360
Tag=

[PopupMenu]
Name=mnuMinifilterInstance
Help=
Index=-1
Menu=刷新FrmListView_mnuMinifilterInstance_mnuRefresh0-10-FrmListView_mnuMinifilterInstance_mnuStep10-10分离实例FrmListView_mnuMinifilterInstance_mnuDetachInstance0-10分离所有实例FrmListView_mnuMinifilterInstance_mnuDetachAllInstances0-10
Left=690
Top=280
Tag=

[PopupMenu]
Name=mnuFileStream
Help=
Index=-1
Menu=刷新FrmListView_mnuFileStream_mnuRefresh0-10-FrmListView_mnuFileStream_mnuStep10-10新建流FrmListView_mnuFileStream_mnuCreateStream0-10从文件复制为流FrmListView_mnuFileStream_mnuCreateStreamFromFile0-10从流复制为文件FrmListView_mnuFileStream_mnuCreateFileFromStream0-10修改流内容FrmListView_mnuFileStream_mnuEditStream0-10删除流FrmListView_mnuFileStream_mnuDeleteStream0-10
Left=290
Top=250
Tag=

[ListView]
Name=ListView
Help=
Index=-1
Style=1 - 报表视图
BStyle=3 - 凹边框
Theme=False
ImageNormalS=无图像列表控件
ImageSmallS=无图像列表控件
SingleSel=False
ShowSel=True
Sort=0 - 不排序
SortColumn=True
NoScroll=False
AutoArrange=False
EditLabels=False
OwnerData=True
AlignTop=False
AlignLeft=False
OwnDraw=False
ColumnHeader=True
ColumnEditor=
NoSortHeader=False
BorderSelect=False
Check=False
FlatScrollBars=True
FullRowSelect=True
GridLines=True
HeaderDragDrop=False
InfoTip=False
LabelTip=False
MultiWorkAreas=False
OneClickActivate=False
Regional=False
SimpleSelect=False
SubItemImages=False
TrackSelect=False
TwoClickActivate=False
UnderlineCold=False
UnderlineHot=False
Enabled=True
Visible=True
ForeColor=SYS,8
BackColor=SYS,5
TextBkColor=SYS,5
Font=微软雅黑,9,0
Left=0
Top=0
Width=770
Height=490
Layout=0 - 不锚定
MousePointer=0 - 默认
Tag=
Tab=True
ToolTip=
ToolTipBalloon=False
AcceptFiles=False


[AllCode]
'这是标准的工程模版，你也可做自己的模版。
'写好工程，复制全部文件到VFB软件文件夹里【template】里即可，子文件夹名为 VFB新建工程里显示的名称
'快去打造属于你自己的工程模版吧。
Type DO_SOMETHING
    ProcessId As DWORD
    TheContext As PVOID
End Type

'Dim Shared CurrentPID As DWORD
'Dim Shared CurrentTID As DWORD
'Dim Shared CurrentProcessName As StringW
Dim Shared prevFrmListViewProc As LONG_PTR = NULL

Dim Shared LastClickedItem As Long = -1 ' 最后点击的行
Dim Shared LastSelectItem As MC_HTREELISTITEM = NULL ' 最后选择的TreeList行
Dim Shared LastClickedSubItem As Long = -1 ' 最后点击的列

Dim Shared BusyDelete As Long = 0

Const MIN_WIDTH = 350
Const MIN_HEIGHT = 500
'Const MAX_WIDTH = 350
'Const MAX_HEIGHT = 500

' 窗口过程：处理消息
Function FrmListView_WndProc(ByVal hWnd As HWND, ByVal uMsg As UINT, ByVal wParam As WPARAM, ByVal lParam As LPARAM) As LRESULT
    Select Case uMsg
        Case WM_SIZING
            ' lParam是RECT结构体指针（拟调整的窗口矩形）
            Dim As RECT Ptr pRect = Cast(RECT Ptr, lParam)
            Dim newWidth As Integer = pRect->right - pRect->left ' 计算拟调整的宽度
            Dim newHeight As Integer = pRect->bottom - pRect->top  ' 计算拟调整的高度
            'Print "WM_SIZE"
            ' 根据拉伸方向（wParam）调整宽度
            Select Case wParam
                ' 1. 仅调整左边界（影响宽度）
                Case WMSZ_LEFT
                    ' 限制宽度在[MIN_WIDTH, MAX_WIDTH]
                    If newWidth < MIN_WIDTH Then
                        pRect->left = pRect->right - MIN_WIDTH  ' 强制左边界右移，保证最小宽度
                    'ElseIf newWidth > MAX_WIDTH Then
                    '    pRect->left = pRect->right - MAX_WIDTH  ' 强制左边界左移，保证最大宽度
                    End If
                
                ' 2. 仅调整右边界（影响宽度）
                Case WMSZ_RIGHT
                    If newWidth < MIN_WIDTH Then
                        pRect->right = pRect->left + MIN_WIDTH  ' 强制右边界左移，保证最小宽度
                    'ElseIf newWidth > MAX_WIDTH Then
                    '    pRect->right = pRect->left + MAX_WIDTH  ' 强制右边界右移，保证最大宽度
                    End If
                
                ' 3. 仅调整上边界（影响高度）
                Case WMSZ_TOP
                    If newHeight < MIN_HEIGHT Then
                        pRect->top = pRect->bottom - MIN_HEIGHT  ' 强制上边界上移，保证最小高度
                    'ElseIf newHeight > MAX_HEIGHT Then
                    '    pRect->top = pRect->bottom - MAX_HEIGHT  ' 强制上边界下移，保证最大高度
                    End If
                
                ' 4. 仅调整下边界（影响高度）
                Case WMSZ_BOTTOM
                    If newHeight < MIN_HEIGHT Then
                        pRect->bottom = pRect->top + MIN_HEIGHT  ' 强制下边界下移，保证最小高度
                    'ElseIf newHeight > MAX_HEIGHT Then
                    '    pRect->bottom = pRect->top + MAX_HEIGHT  ' 强制下边界上移，保证最大高度
                    End If
                
                ' 5. 调整左上角（同时影响宽度和高度）
                Case WMSZ_TOPLEFT
                    ' 先限制宽度
                    If newWidth < MIN_WIDTH Then
                        pRect->left = pRect->right - MIN_WIDTH
                    'ElseIf newWidth > MAX_WIDTH Then
                    '    pRect->left = pRect->right - MAX_WIDTH
                    End If
                    ' 再限制高度（需重新计算宽高，因为left可能已修改）
                    newHeight = pRect->bottom - pRect->top  ' 重新计算高度
                    If newHeight < MIN_HEIGHT Then
                        pRect->top = pRect->bottom - MIN_HEIGHT
                    'ElseIf newHeight > MAX_HEIGHT Then
                    '    pRect->top = pRect->bottom - MAX_HEIGHT
                    End If
                
                ' 6. 调整右上角（同时影响宽度和高度）
                Case WMSZ_TOPRIGHT
                    ' 先限制宽度
                    If newWidth < MIN_WIDTH Then
                        pRect->right = pRect->left + MIN_WIDTH
                    ElseIf newWidth > MAX_WIDTH Then
                        pRect->right = pRect->left + MAX_WIDTH
                    End If
                    ' 再限制高度
                    newHeight = pRect->bottom - pRect->top
                    If newHeight < MIN_HEIGHT Then
                        pRect->top = pRect->bottom - MIN_HEIGHT
                    'ElseIf newHeight > MAX_HEIGHT Then
                    '    pRect->top = pRect->bottom - MAX_HEIGHT
                    End If
                
                ' 7. 调整左下角（同时影响宽度和高度）
                Case WMSZ_BOTTOMLEFT
                    ' 先限制宽度
                    If newWidth < MIN_WIDTH Then
                        pRect->left = pRect->right - MIN_WIDTH
                    'ElseIf newWidth > MAX_WIDTH Then
                    '    pRect->left = pRect->right - MAX_WIDTH
                    End If
                    ' 再限制高度
                    newHeight = pRect->bottom - pRect->top
                    If newHeight < MIN_HEIGHT Then
                        pRect->bottom = pRect->top + MIN_HEIGHT
                    'ElseIf newHeight > MAX_HEIGHT Then
                    '    pRect->bottom = pRect->top + MAX_HEIGHT
                    End If
                
                ' 8. 调整右下角（同时影响宽度和高度）
                Case WMSZ_BOTTOMRIGHT
                    ' 先限制宽度
                    If newWidth < MIN_WIDTH Then
                        pRect->right = pRect->left + MIN_WIDTH
                    'ElseIf newWidth > MAX_WIDTH Then
                    '    pRect->right = pRect->left + MAX_WIDTH
                    End If
                    ' 再限制高度
                    newHeight = pRect->bottom - pRect->top
                    If newHeight < MIN_HEIGHT Then
                        pRect->bottom = pRect->top + MIN_HEIGHT
                    'ElseIf newHeight > MAX_HEIGHT Then
                    '    pRect->bottom = pRect->top + MAX_HEIGHT
                    End If
            End Select
            
            Return 1  ' 通知系统使用修改后的矩形
    End Select
    Return CallWindowProc(Cast(WndProc, prevFrmListViewProc), hWnd, uMsg, wParam, lParam)
End Function

'[FrmListView]事件 : 活动状态（被激活或失去焦点）
'hWndForm    当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'fActive     =0 失去焦点 <>0 得到焦点
'Sub FrmListView_WM_NcActivate(hWndForm As hWnd, fActive As Long)
'    Dim CurrentIndex As Integer = GetIndexByListViewHwnd(hWndForm)
'    Me.hWndForm = hWndForm
'    ListView.hWndForm = hWndForm
'    If fActive = 0 Then
'        'Print "[FrmListView]失去焦点," & FrmListView.hWnd & "->" & CurrentInformation.intType & " CurrentIndex:" & CurrentIndex
'        'CurrentInformationArray(CurrentIndex).hListView = hWndForm
'        CurrentInformationArray(CurrentIndex).intType = CurrentInformation.intType
'        CurrentInformationArray(CurrentIndex).ProcessId = CurrentPID
'        
'        CurrentInformationArray(CurrentIndex).CurrentModule.ModuleName = CurrentInformation.CurrentModule.ModuleName
'        CurrentInformationArray(CurrentIndex).CurrentModule.ModuleHandle = CurrentInformation.CurrentModule.ModuleHandle
'        
'        CurrentInformationArray(CurrentIndex).CurrentDriver.DriverBase = CurrentInformation.CurrentDriver.DriverBase
'        CurrentInformationArray(CurrentIndex).CurrentDriver.DriverSize = CurrentInformation.CurrentDriver.DriverSize
'        CurrentInformationArray(CurrentIndex).CurrentDriver.DriverObject = CurrentInformation.CurrentDriver.DriverObject
'        CurrentInformationArray(CurrentIndex).CurrentDriver.DriverName = CurrentInformation.CurrentDriver.DriverName
'        CurrentInformationArray(CurrentIndex).CurrentDriver.DriverPath = CurrentInformation.CurrentDriver.DriverPath
'    Else
'        'Print "[FrmListView]得到焦点," & FrmListView.hWnd & "->" & CurrentInformation.intType & " CurrentIndex:" & CurrentIndex
'        If CurrentIndex = -1 Then
'            For i As Integer = 0 To UBound(CurrentInformationArray)
'                If CurrentInformationArray(i).hListView = NULL Then
'                    CurrentIndex = i
'                    'Print "[FrmListView]得到焦点后," & FrmListView.hWnd & "->" & CurrentInformation.intType'与FrmMain的初始化同理
'                    Exit For
'                End If
'            Next
'            If CurrentIndex = -1 Then
'                CurrentIndex = UBound(CurrentInformationArray) + 1
'                ReDim Preserve CurrentInformationArray(UBound(CurrentInformationArray) + 5) As CURRENT_INFORMATION ' 开第二个不同FrmListView时崩溃
'            End If
'            'Print "UBound(CurrentInformationArray):" & UBound(CurrentInformationArray) & " CurrentIndex:" & CurrentIndex
'            CurrentInformationArray(CurrentIndex).hListView = hWndForm
'        Else
'            'memcpy @CurrentInformation, @CurrentInformationArray(CurrentIndex), SizeOf(CurrentInformation)
'            CurrentInformation.intType = CurrentInformationArray(CurrentIndex).intType
'            CurrentPID = CurrentInformationArray(CurrentIndex).ProcessId
'            
'            CurrentInformation.CurrentModule.ModuleName = CurrentInformationArray(CurrentIndex).CurrentModule.ModuleName
'            CurrentInformation.CurrentModule.ModuleHandle = CurrentInformationArray(CurrentIndex).CurrentModule.ModuleHandle
'            
'            CurrentInformation.CurrentDriver.DriverBase = CurrentInformationArray(CurrentIndex).CurrentDriver.DriverBase
'            CurrentInformation.CurrentDriver.DriverSize = CurrentInformationArray(CurrentIndex).CurrentDriver.DriverSize
'            CurrentInformation.CurrentDriver.DriverObject = CurrentInformationArray(CurrentIndex).CurrentDriver.DriverObject
'            CurrentInformation.CurrentDriver.DriverName = CurrentInformationArray(CurrentIndex).CurrentDriver.DriverName
'            CurrentInformation.CurrentDriver.DriverPath = CurrentInformationArray(CurrentIndex).CurrentDriver.DriverPath
'            'Print "[FrmListView]得到焦点后," & FrmListView.hWnd & "->" & CurrentInformation.intType'与FrmMain的初始化同理
'        End If
'   End If
'End Sub

'[FrmListView.TxtFilter1]事件 : 文本已经被修改（修改前用 EN_UPDATE
'hWndForm    当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'hWndControl 当前控件的句柄(也是窗口句柄，如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 )
Sub FrmListView_TxtFilter1_EN_Change(hWndForm As hWnd, hWndControl As hWnd)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    
    lblNum.Caption = "正在筛选..."
    SetListViewFilter ListView, TxtFilter1.Text
    CommitListViewView ListView
    lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
End Sub

'[FrmListView]事件 : 自定义消息（全部消息），在其它事件处理后才轮到本事件。
'hWndForm    当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'wMsg        消息值，0--&H400 由WIN系统定义，用户自定义是 WM_USER+** 定义。例 PostMessage( 窗口句柄 , Msg , wParam , lParam ) 
'wParam      主消息参数，什么作用，由发送消息者定义
'lParam      副消息参数，什么作用，由发送消息者定义
Function FrmListView_Custom(hWndForm As hWnd, wMsg As UInteger, wParam As wParam, lParam As lParam) As LResult
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Select Case wMsg
    Case WM_NOTIFY
        Dim Nmhdr As NMHDR Ptr = Cast(NMHDR Ptr, lParam)
        If Nmhdr->hwndFrom = ListView.hWnd Then
            
            Select Case Nmhdr->code
            Case LVN_GETDISPINFO
                Dim p As NMLVDISPINFO Ptr = Cast(NMLVDISPINFO Ptr, lParam)
                If BusyDelete <> 0 Then
                    'Print "发生重入!"
                    p->item.pszText = @WStr("")
                    Return 0
                End If
                
                ' 获取当前窗口的私有ctx（唯一渲染入口）
                Dim ctx As ListViewContext Ptr = Cast(ListViewContext Ptr, GetWindowLongPtr(p->hdr.hwndFrom, GWLP_USERDATA))
                If ctx = NULL Then Return 0
                If ctx->Snap = NULL Then Return 0
                If p->item.iItem < 0 Then Return 0
                If p->item.iItem >= ctx->VisibleCount Then Return 0

                Dim real As Long = ctx->VisibleIndex(p->item.iItem)
                If real < 0 OrElse real >= ctx->Snap->RowCount Then Return 0
                
                Dim r As LVRow Ptr = ctx->Snap->Rows[real]
                If r = NULL Then Return 0

                If (p->item.mask And LVIF_TEXT) <> 0 Then
                    If p->item.iSubItem < r->ColCount Then
                        p->item.pszText = r->Cols[p->item.iSubItem]
                    Else
                        p->item.pszText = @WStr("")
                    End If
                    p->item.cchTextMax = lstrlenW(p->item.pszText) + 1
                End If
                Return 0
            End Select
        End If

    Case NM_CUSTOMDRAW
        If BusyDelete <> 0 Then Return CDRF_DODEFAULT
        
        Dim cd As NMLVCUSTOMDRAW Ptr = Cast(NMLVCUSTOMDRAW Ptr, lParam)
        Dim ctx As ListViewContext Ptr = _
            Cast(ListViewContext Ptr, GetWindowLongPtr(cd->nmcd.hdr.hwndFrom, GWLP_USERDATA))
        
        ' 先校验上下文和快照有效性
        If ctx = NULL OrElse ctx->Snap = NULL Then Return CDRF_DODEFAULT
        
        Select Case cd->nmcd.dwDrawStage
            Case CDDS_PREPAINT
                Return CDRF_NOTIFYITEMDRAW
            Case CDDS_ITEMPREPAINT
                Return CDRF_NOTIFYSUBITEMDRAW
            Case CDDS_ITEMPREPAINT Or CDDS_SUBITEM
                ' 新增：完整边界校验
                Dim itemIdx As DWORD = cd->nmcd.dwItemSpec
                If itemIdx < 0 OrElse itemIdx >= ctx->VisibleCount Then
                    Return CDRF_DODEFAULT
                End If
                
                Dim real As Long = ctx->VisibleIndex(itemIdx)
                If real < 0 OrElse real >= ctx->Snap->RowCount Then
                    Return CDRF_DODEFAULT
                End If
                
                Dim r As LVRow Ptr = ctx->Snap->Rows[real]
                If r = NULL Then Return CDRF_DODEFAULT
                
                cd->clrText   = r->Fore
                cd->clrTextBk = r->Back
                Return CDRF_DODEFAULT
        End Select
        
    Case WM_SignVerify
        Dim pMsg As SIGNVERIFY_MSG Ptr = Cast(SIGNVERIFY_MSG Ptr, lParam)
        If pMsg <> 0 Then
            If pMsg->ctrlType = "ListView" Then
                ' 更新 ListView 的颜色
                'Print "开始更新 ListView 的颜色..."
                SetItemColor ListView, pMsg->rowIndex, pMsg->ForeColor, pMsg->BackColor
                'ListView.Refresh
                Deallocate pMsg
            End If
        End If
        
        ' 延迟刷新
        Static LastRefresh As ULong = 0
        If GetTickCount() - LastRefresh > 50 Then
            ListView.Refresh
            LastRefresh = GetTickCount()
        End If
    End Select
    Return False
End Function

'[FrmListView]事件 : 窗口已经改变了大小
'hWndForm  当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'fwSizeType = SIZE_MAXHIDE     SIZE_MAXIMIZED   SIZE_MAXSHOW    SIZE_MINIMIZED    SIZE_RESTORED  
''            其他窗口最大化   窗口已最大化     其他窗口恢复    窗口已最小化      窗口已调整大小
'nWidth nHeight  是客户区大小，不是全部窗口大小。
Sub FrmListView_WM_Size(hWndForm As hWnd, fwSizeType As Long, nWidth As Long, nHeight As Long)
    Static Inited As Boolean
    If fwSizeType = SIZE_RESTORED And Inited Then
        ' 调整X轴方向
        ListView.Width = nWidth
        TxtFilter1.Width = nWidth - lblNum.Width
        lblNum.Left = TxtFilter1.Width
        
        ' 调整Y轴方向
        ListView.Height = nHeight - ListView.Top - TxtFilter1.Height
        TxtFilter1.Top = nHeight - TxtFilter1.Height
        lblNum.Top = TxtFilter1.Top
    End If
    Inited = True
End Sub

'[FrmListView.ListView]事件 : 键盘按下
'【终极兼容版 - 无FocusSubItem、无鼠标、100%稳定】
Sub FrmListView_ListView_LVN_KeyDown(hWndForm As hWnd, hWndControl As hWnd, pNKD As LV_KEYDOWN)
    ' 判断 Ctrl + C
    If pNKD.wVKey = Asc("C") Then
        If (GetKeyState(VK_CONTROL) And &H8000) <> 0 Then
            'Print "按下Ctrl+C"

            ' 打印调试
            'Print "最终索引："; LastClickedItem; "  "; LastClickedSubItem

            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
        End If
    End If
End Sub

'[FrmListView.ListView]事件 : 按下鼠标右键
'hWndForm    当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'hWndControl 当前控件的句柄(也是窗口句柄，如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 )
'MouseFlags  MK_CONTROL   MK_LBUTTON     MK_MBUTTON     MK_RBUTTON    MK_SHIFT     MK_XBUTTON1       MK_XBUTTON2 
''           CTRL键按下   鼠标左键按下   鼠标中键按下   鼠标右键按下  SHIFT键按下  第一个X按钮按下   第二个X按钮按下
'检查什么键按下用  If (MouseFlags And MK_CONTROL)<>0 Then CTRL键按下 
'xPos yPos   当前鼠标位置，相对于控件。就是在控件里的坐标。
Sub FrmListView_ListView_WM_RButtonDown(hWndForm As hWnd, hWndControl As hWnd, MouseFlags As Long, xPos As Long, yPos As Long)
    Dim lvinfo As LVHITTESTINFO
    lvinfo.pt.x = xPos
    lvinfo.pt.y = yPos
    ScreenToClient hWndControl, @CurrentPos
    ListView_SubItemHitTest(hWndControl, @lvinfo)
    If (lvinfo.iItem >= 0 AndAlso lvinfo.iItem <= ListView.ItemCount - 1) AndAlso _
    (lvinfo.iSubItem >= 0 AndAlso lvinfo.iSubItem <= ListView.ColumnCount - 1) Then
        LastClickedItem = lvinfo.iItem
        LastClickedSubItem = lvinfo.iSubItem
    End If
End Sub

'[FrmListView.ListView]事件 : 按下鼠标左键
'hWndForm    当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'hWndControl 当前控件的句柄(也是窗口句柄，如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 )
'MouseFlags  MK_CONTROL   MK_LBUTTON     MK_MBUTTON     MK_RBUTTON    MK_SHIFT     MK_XBUTTON1       MK_XBUTTON2 
''           CTRL键按下   鼠标左键按下   鼠标中键按下   鼠标右键按下  SHIFT键按下  第一个X按钮按下   第二个X按钮按下
'检查什么键按下用  If (MouseFlags And MK_CONTROL)<>0 Then CTRL键按下 
'xPos yPos   当前鼠标位置，相对于控件。就是在控件里的坐标。
Sub FrmListView_ListView_WM_LButtonDown(hWndForm As hWnd, hWndControl As hWnd, MouseFlags As Long, xPos As Long, yPos As Long)
    Dim lvinfo As LVHITTESTINFO
    lvinfo.pt.x = xPos
    lvinfo.pt.y = yPos
    ScreenToClient hWndControl, @CurrentPos
    ListView_SubItemHitTest(hWndControl, @lvinfo)
    If (lvinfo.iItem >= 0 AndAlso lvinfo.iItem <= ListView.ItemCount - 1) AndAlso _
    (lvinfo.iSubItem >= 0 AndAlso lvinfo.iSubItem <= ListView.ColumnCount - 1) Then
        LastClickedItem = lvinfo.iItem
        LastClickedSubItem = lvinfo.iSubItem
    End If
End Sub

'[FrmListView.ListView]事件 : 一个列被点击了
'hWndForm    当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'hWndControl 当前控件的句柄(也是窗口句柄，如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 )
'pNMV.iSubItem  当前被点击列的索引。lpNMV.iItem = -1 其它成员均为零 
Sub FrmListView_ListView_LVN_ColumnClick(hWndForm As hWnd, hWndControl As hWnd, pNMV As NM_LISTVIEW)
    ' 1. 获取ListView上下文（和你LVN_GETDISPINFO里的获取方式一致）
    Dim ctx As ListViewContext Ptr = _
        Cast(ListViewContext Ptr, GetWindowLongPtr(hWndControl, GWLP_USERDATA))
    
    ' 2. 安全校验（防止空指针崩溃）
    If ctx = NULL Then Exit Sub
    If ctx->Snap = NULL Then Exit Sub
    If ctx->VisibleCount <= 1 Then Exit Sub ' 不足1行无需排序
    If pNMV.iSubItem < 0 Then Exit Sub      ' 无效列索引
    
    ' 3. 更新排序状态
    ' 同列重复点击 → 切换升降序
    If ctx->SortedColumn = pNMV.iSubItem Then
        ctx->SortAscending = Not ctx->SortAscending
    Else
        ' 新列点击 → 默认升序
        ctx->SortedColumn = pNMV.iSubItem
        ctx->SortAscending = True
    End If
    
    ' 4. 执行排序（核心排序函数，复用之前的逻辑）
    SortListViewVisibleIndex(ctx)
    
    ' 5. 刷新虚拟ListView（必须手动刷新，否则界面不更新）
    ListView_RedrawItems(hWndControl, 0, ctx->VisibleCount - 1)
End Sub

'[FrmListView]事件 : 窗口完全显示后。
'hWndForm  当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'UserData  来自显示窗口最后1个参数，例： Form2.Show(父窗口句柄,模式,UserData)
Sub FrmListView_Shown(hWndForm As hWnd, UserData As Integer)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, UserData)
    Me.UserData(0) = UserData
    CurrentInfo->hListView = Me.hWnd
    
    ListView_SetExtendedListViewStyleEx(ListView.hWnd, LVS_EX_DOUBLEBUFFER, LVS_EX_DOUBLEBUFFER)
    ListView_SetExtendedListViewStyleEx(ListView.hWnd, LVS_EX_INFOTIP, LVS_EX_INFOTIP)
    UninitListViewData ListView
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Dim CurrentTID As DWORD = CurrentInfo->ThreadId
    
    Dim CurrentProcessName As StringW = GetNameByPath(GetProcessPath(CurrentInfo->ProcessId))
    lblNum.Caption = "正在获取..."
    InitializeListView CurrentInfo->intType, ListView ' 弹出的一般都是ListView
    
    Select Case CurrentInfo->intType
        Case Module
            GetModuleList CurrentPID, ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 模块列表"
        Case Thread
            GetThreadList CurrentPID, ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 线程列表"
        Case KernelThread
            GetKernelThreadList ListView
            Me.Caption = "[" & CurrentInfo->CurrentDriver.DriverName & "]" & " 线程列表"
        Case Windows
            GetWindowList CurrentPID, ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 窗口列表"
        Case ProcessTimer
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetProcessTimerList False, CurrentPID, ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 定时器列表"
        Case Handles
            If (Not IsDriverLoaded) AndAlso (AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES) Then
                If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" ' : Exit Sub
            End If
            GetHandleList CurrentPID, ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 句柄列表"
            'Print "1"'下面的Print和SetListViewData在执行此次Case后均不会被执行
            'Print "2"
        Case Memory
            GetMemoryBlockList CurrentPID,ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 内存块列表"
        Case WebConnect
            GetWebConnectList CurrentPID, ListView, True
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 网络连接列表"
        Case Privilege
            GetProcessPrivilegeList CurrentPID, ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 特权列表"
        Case Hook
            If CurrentInformation.CurrentModule.ModuleHandle = NULL Then
                GetProcessHookList CurrentPID, ListView
            Else
                GetModuleHookList CurrentPID, CurrentInformation.CurrentModule.ModuleName, CurrentInformation.CurrentModule.ModuleHandle, ListView
            End If
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " Hook列表(inline hook, iat hook, eat hook)"
            
        Case ThreadCallStack
            GetCallStack CurrentPID, CurrentTID, ListView
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & " TID:" & CurrentTID & "]" & " 调用栈"
        Case MsgHook
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetMsgHookList False, CurrentPID, ListView
            If ListView.ItemCount = 0 Then AfxMsg "枚举失败,可以尝试重新枚举"
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 消息钩子列表"
        Case EventHook
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetEventHookList False, CurrentPID, ListView
            If ListView.ItemCount = 0 Then AfxMsg "枚举失败,可以尝试重新枚举"
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 事件钩子列表"
        Case Hotkey
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetHotkeyList False, CurrentPID, ListView
            If ListView.ItemCount = 0 Then AfxMsg "枚举失败,可以尝试重新枚举"
            Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 热键列表"
        Case UnlockTheFile
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            Unlock_File1 ListView', FilePathByUnlock
            Me.Caption = "文件占用查看小工具"
        Case IOFunction
            If CurrentInfo->CurrentDriver.DriverObject <> 0 Then
                GetDriverIOFunctionList ListView, CurrentInfo->CurrentDriver.DriverPath, CurrentInfo->CurrentDriver.DriverObject
            End If
            Me.Caption = "[" & CurrentInfo->CurrentDriver.DriverName & "]" & " IO派遣函数列表"
        Case MinifilterInstances
            EnumMinifilterInstances ListView, @CurrentInfo->CurrentDriver.DriverName
            Me.Caption = "[" & CurrentInfo->CurrentDriver.DriverName & "]" & " 实例列表"
        Case FileStream
            EnumerateFileStreams ListView, CurrentInfo->FilePath
            Me.Caption = "[" & CurrentInfo->FilePath & "]" & " 流列表"
    End Select
    lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
End Sub

'[FrmListView]事件 : 即将关闭窗口，返回非0可阻止关闭
'hWndForm  当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
Function FrmListView_WM_Close(hWndForm As hWnd) As LResult
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Deallocate CurrentInfo
    UninitListViewData ListView
    Function = FALSE ' 返回 TRUE 阻止关闭窗口。
End Function

'[FrmListView.ListView]事件 : 鼠标右键单击
'hWndForm    当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
'hWndControl 当前控件的句柄(也是窗口句柄，如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 )
'xPos yPos   当前鼠标位置，相对于屏幕。就是屏幕坐标。
Sub FrmListView_ListView_WM_ContextMenu(hWndForm As hWnd, hWndControl As hWnd, xPos As Long, yPos As Long)
    Me.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    CurrentPos.x = xPos
    CurrentPos.y = yPos
    ScreenToClient hWndControl, @CurrentPos
    Select Case CurrentInfo->intType
        Case Module
            mnuModule.hWndForm = hWndForm
            PopupMenu hWndForm,mnuModule.HMENU
        Case Thread, KernelThread
            mnuThread.hWndForm = hWndForm
            PopupMenu hWndForm, mnuThread.HMENU
        Case Memory
            mnuMemory.hWndForm = hWndForm
            PopupMenu hWndForm, mnuMemory.HMENU
        Case Privilege
            mnuPrivilege.hWndForm = hWndForm
            PopupMenu hWndForm, mnuPrivilege.HMENU
        Case WebConnect
            mnuWebConnect.hWndForm = hWndForm
            PopupMenu hWndForm, mnuWebConnect.HMENU
        Case Windows
            mnuWindow.hWndForm = hWndForm
            PopupMenu hWndForm, mnuWindow.HMENU
        Case Handles
            mnuHandle.hWndForm = hWndForm
            PopupMenu hWndForm, mnuHandle.HMENU
        Case ProcessTimer
            mnuProcessTimer.hWndForm = hWndForm
            PopupMenu hWndForm, mnuProcessTimer.HMENU
        Case MsgHook
            mnuWindow.hWndForm = hWndForm
            PopupMenu hWndForm, mnuMsgHook.HMENU
        Case EventHook
            mnuHandle.hWndForm = hWndForm
            PopupMenu hWndForm, mnuEventHook.HMENU
        Case Hotkey
            mnuHandle.hWndForm = hWndForm
            PopupMenu hWndForm, mnuHotkey.HMENU
        Case UnlockTheFile
            mnuUnlock_File.hWndForm = hWndForm
            PopupMenu hWndForm, mnuUnlock_File.HMENU
            
        Case FileStream
            mnuFileStream.hWndForm = hWndForm
            PopupMenu hWndForm, mnuFileStream.HMENU
    End Select
End Sub

'[FrmListView.mnuThread]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuThread_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Select Case wID
        Case FrmListView_mnuThread_mnuRefreshThread ' 刷新线程
            lblNum.Caption = "正在刷新..."
            'DeleteAllItemsEx ListView
            GetThreadList CurrentPID, ListView
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuThread_mnuTerminateThread ' 结束线程
            For i As Integer = ListView.ItemCount - 1 To 0 Step -1
                If IsListViewItemSelected(ListView, i) Then
                    If KillThread(ValULng(ListView.GetItemText(i, 0))) Then DeleteItemEx ListView, i, False
                End If
            Next
            CommitListViewView ListView
        Case FrmListView_mnuThread_mnuForceKillThread ' 强制结束线程
            For i As Integer = ListView.ItemCount - 1 To 0 Step -1
                If IsListViewItemSelected(ListView, i) Then
                    'Print "TID:" & Cast(HANDLE, ValULng(ListView.GetItemText(i, 0)))
                    If ForceKillThread(Cast(HANDLE, ValULng(ListView.GetItemText(i, 0)))) Then DeleteItemEx ListView, i, False
                End If
            Next
            CommitListViewView ListView
        Case FrmListView_mnuThread_mnuForceDestroyThread ' 暴力摧毁线程(可能蓝屏)
            For i As Integer = ListView.ItemCount - 1 To 0 Step -1
                If IsListViewItemSelected(ListView, i) Then
                    Dim TID As HANDLE = Cast(HANDLE, ValULng(ListView.GetItemText(i, 0)))
                    'Print "TID:" & TID
                    If IoControl(hDrv, IOCTL_ForceDestroyThread, @TID, SizeOf(TID)) <> 0 Then DeleteItemEx ListView, i, False
                End If
            Next
            CommitListViewView ListView
        Case FrmListView_mnuThread_mnuSuspendThread ' 挂起线程
            For i As Integer = 0 To ListView.ItemCount - 1
                If IsListViewItemSelected(ListView, i) Then MySuspendThread ValULng(ListView.GetItemText(i, 0))
            Next
            GetThreadList CurrentPID, ListView
        Case FrmListView_mnuThread_mnuResumeThread ' 恢复线程
            For i As Integer = 0 To ListView.ItemCount - 1
                If IsListViewItemSelected(ListView, i) Then MyResumeThread ValULng(ListView.GetItemText(i, 0))
            Next
            GetThreadList CurrentPID, ListView
        Case FrmListView_mnuThread_mnuViewThreadStack ' 查看线程栈
            Dim CurrentInfo_New As CURRENT_INFORMATION Ptr = Allocate(SizeOf(CURRENT_INFORMATION))
            CurrentInfo_New->ThreadId = ValInt(ListView.GetItemText(ListView.SelectedItem, 0))
            CurrentInfo_New->intType = ThreadCallStack
            FrmListView.Show,, Cast(Integer, CurrentInfo_New)
        Case FrmListView_mnuThread_mnuEditMemory ' 查看/编辑内存
            If LastClickedSubItem <> 3 AndAlso LastClickedSubItem <> 4 AndAlso LastClickedSubItem <> 5 Then Exit Sub
            Dim Addr As ULONG64 = ValULng(FF_Replace(ListView.GetItemText(LastClickedItem, LastClickedSubItem), "0x", "&H"))
            Dim MemoryInfo As MemoryStruct Ptr = Allocate(SizeOf(MemoryStruct))
            MemoryInfo->Addr = Cast(PVOID, Addr)
            MemoryInfo->dwProcessId = Cast(HANDLE, Cast(ULONG64, CurrentPID))
            FrmMemoryEditor.Show,, Cast(Integer, MemoryInfo)
        Case FrmListView_mnuThread_mnuLittleCopy ' 复制单格数据
            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
        Case FrmListView_mnuThread_mnuLocateFilePath ' 定位文件位置(资源管理器)
            Dim lpFilePath As StringW = ListView.GetItemText(ListView.SelectedItem, 6)
            If Not LocateFilePathByExplorer(lpFilePath) Then AfxMsg "定位失败!", "提示"
    End Select
End Sub

'[FrmListView.mnuModule]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuModule_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Dim ModuleHandle As HMODULE = Cast(Any Ptr, ValULng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 2), "0x", "&H")))
    Select Case wID
        Case FrmListView_mnuModule_mnuRefreshModule ' 刷新
            lblNum.Caption = "正在刷新..."
            GetModuleList CurrentPID, ListView
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuModule_mnuUnloadModule ' 卸载模块
            If UnloadModule(CurrentPID, ModuleHandle) Then
                AfxMsg "卸载成功!"
                DeleteItemEx ListView, ListView.SelectedItem
            Else
                AfxMsg "卸载失败!是否强制卸载?"
            End If
        Case FrmListView_mnuModule_mnuForceUnloadModule ' 强制卸载模块
            If ForceUnloadModule(CurrentPID, ModuleHandle) Then
                AfxMsg "卸载成功!"
                DeleteItemEx ListView, ListView.SelectedItem
            Else
                AfxMsg "卸载失败!"
            End If
        Case FrmListView_mnuModule_mnuViewHook ' 查看Hook
            Dim CurrentInfo_New As CURRENT_INFORMATION Ptr = Allocate(SizeOf(CURRENT_INFORMATION))
            CurrentInfo_New->intType = Hook
            CurrentInfo_New->ProcessId = CurrentPID
            CurrentInfo_New->CurrentModule.ModuleName = ListView.GetItemText(ListView.SelectedItem, 1)
            CurrentInfo_New->CurrentModule.ModuleHandle = ModuleHandle
            FrmListView.Show,, Cast(Integer, CurrentInfo_New)
        Case FrmListView_mnuModule_mnuEditMemory ' 查看/编辑内存
            If LastClickedSubItem <> 2 Then Exit Sub
            Dim Addr As ULONG64 = ValULng(FF_Replace(ListView.GetItemText(LastClickedItem, LastClickedSubItem), "0x", "&H"))
            Dim MemoryInfo As MemoryStruct Ptr = Allocate(SizeOf(MemoryStruct))
            MemoryInfo->Addr = Cast(PVOID, Addr)
            MemoryInfo->dwProcessId = Cast(HANDLE, Cast(ULONG64, CurrentPID))
            FrmMemoryEditor.Show,, Cast(Integer, MemoryInfo)
        Case FrmListView_mnuModule_mnuLittleCopy ' 复制单格数据
            'Print "最终索引："; LastClickedItem; "  "; LastClickedSubItem
            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
        Case FrmListView_mnuModule_mnuLocateFilePath ' 定位文件位置(资源管理器)
            Dim lpFilePath As StringW = ListView.GetItemText(ListView.SelectedItem, 4)
            If Not LocateFilePathByExplorer(lpFilePath) Then AfxMsg "定位失败!", "提示"
   End Select
End Sub

'[FrmListView.mnuUnlock_FIle]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuUnlock_File_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Select Case wID
        Case FrmListView_mnuUnlock_File_mnuRefresh '刷新列表
            lblNum.Caption = "正在刷新..."
            Unlock_File ListView
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuUnlock_File_mnuCloseHandle ' 关闭句柄
            'MyLog.PrintLog LOG_INFO,,, "开始关闭句柄..."
            For i As Integer = ListView.ItemCount - 1 To 0 Step -1
                If IsListViewItemSelected(ListView, i) Then
                    'MyLog.PrintLog LOG_INFO,,, "找到句柄"
                    If CloseProcessHandle(ValUInt(ListView.GetItemText(i, 1)), Cast(HANDLE, ValULng(FF_Replace(ListView.GetItemText(i, 4), "0x", "&H")))) Then
                        'MyLog.PrintLog LOG_INFO,,, "已关闭句柄"
                        BusyDelete += 1
                        DeleteItemEx ListView, i, False
                        BusyDelete -= 1
                        AfxMsg "关闭句柄成功!", "提示"
                    Else
                        'AfxMsg "关闭句柄失败!", "提示"
                    End If
                End If
            Next
            lblNum.Caption = "数量:" & ListView.ItemCount
            CommitListViewView ListView
        Case FrmListView_mnuUnlock_File_mnuForceCloseHandle ' 强制关闭句柄
            '&H863
            Dim DoSomething As DO_SOMETHING
            DoSomething.ProcessId = ValULng(ListView.GetItemText(ListView.SelectedItem, 1))
            DoSomething.TheContext = Cast(HANDLE, ValULng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 4), "0x", "&H")))
            IoControl hDrv, IOCTL_CloseHandle, @DoSomething, SizeOf(DO_SOMETHING)
            If (GetLastError = 0) Then
                AfxMsg "强制关闭句柄成功!", "提示"
                DeleteItemEx ListView, ListView.SelectedItem
            Else
                AfxMsg "强制关闭句柄失败!", "提示"
                MyLog.PrintWin32Error "FrmListView_mnuUnlock_FIle_mnuForceCloseHandle", "IOCTL_CloseHandle"
            End If
        Case FrmListView_mnuUnlock_File_mnuTerminateProcess ' 结束进程
            KillProcess ValULng(ListView.GetItemText(ListView.SelectedItem, 1))
        Case FrmListView_mnuUnlock_File_mnuLittleCopy ' 复制单格数据
            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
   End Select
End Sub

'[FrmListView.mnuMemory]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuMemory_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Select Case wID
        Case FrmListView_mnuMemory_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            GetMemoryBlockList CurrentPID, ListView
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuMemory_mnuLocateMemory ' 定位内存
            Dim LocatedAddr As UInteger = ValULng(FF_Replace(AfxInputBox(,,, "定位内存", "请输出需要定位的内存地址:(例:0x7FFC18B9C000)"), "0x", "&H"))
            Dim Addr As UInteger, Size As ULong
            For i As Integer = 0 To ListView.ItemCount - 1
                Addr = ValULng(FF_Replace(ListView.GetItemText(i, 0), "0x", "&H"))
                Size = ValUInt(FF_Replace(ListView.GetItemText(i, 1), "0x", "&H"))
                'Print "Addr:0x" & WHex(Addr) & " Size:0x" & WHex(Size) & " LocatedAddr:0x" & WHex(LocatedAddr) & " Addr+Size:0x" & WHex(Addr + Size)
                If Addr <= LocatedAddr AndAlso Addr + Size >= LocatedAddr Then ' 定位成功
                    'Print "找到"
                    ListView.UnselectAllItems
                    ListView.SelectedItem = i
                    ListView.ScrollSelectedItem
                    Exit For
                End If
            Next
        Case FrmListView_mnuMemory_mnuChangeMemory ' 修改内存属性
            Dim mc As MEMORY_CHANGE Ptr = Allocate(SizeOf(MEMORY_CHANGE))
            mc->ProcessId = CurrentPID
            mc->Addr = ValULng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 0), "0x", "&H"))
            mc->dwSize = ValUInt(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 1), "0x", "&H"))
            Dim strProtect As StringW = ListView.GetItemText(ListView.SelectedItem, 2)
            mc->OldProtect = ParseProtectString(strProtect)
            FrmMemoryChange.Show,, Cast(Integer, mc)
        ' 搜索内存
        Case FrmListView_mnuMemory_mnuEditMemory ' 查看/编辑内存
            Dim Addr As ULONG64 = ValULng(FF_Replace(ListView.GetItemText(LastClickedItem, 0), "0x", "&H"))
            Dim MemoryInfo As MemoryStruct Ptr = Allocate(SizeOf(MemoryStruct))
            MemoryInfo->Addr = Cast(PVOID, Addr)
            MemoryInfo->dwProcessId = Cast(HANDLE, Cast(ULONG64, CurrentPID))
            FrmMemoryEditor.Show,, Cast(Integer, MemoryInfo)
        Case FrmListView_mnuMemory_mnuLittleCopy ' 复制单格数据
            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
        Case FrmListView_mnuMemory_mnuLocateFilePath ' 定位文件位置(资源管理器)
            Dim lpFilePath As StringW = ListView.GetItemText(ListView.SelectedItem, 6)
            If Not LocateFilePathByExplorer(lpFilePath) Then AfxMsg "定位失败!", "提示"
   End Select
End Sub

'[FrmListView.mnuHandle]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuHandle_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Select Case wID
        Case FrmListView_mnuHandle_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            GetHandleList CurrentPID, ListView
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuHandle_mnuCloseHandle ' 关闭
            For i As Integer = ListView.ItemCount - 1 To 0 Step -1
                If IsListViewItemSelected(ListView, i) Then
                    CloseProcessHandle ValULng(ListView.GetItemText(i, 1)), Cast(HANDLE, ValULng(ListView.GetItemText(i, 4)))
                    DeleteItemEx ListView, i
                End If
            Next
            CommitListViewView ListView
        Case FrmListView_mnuHandle_mnuLittleCopy ' 复制单格数据
            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
   End Select
End Sub

'[FrmListView.mnuWebConnect]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuWebConnect_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Select Case wID
        Case FrmListView_mnuWebConnect_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            GetWebConnectList CurrentPID, ListView, Not GetMenuCheckState(mnuWebConnect, FrmListView_mnuWebConnect_mnuViewAll)
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuWebConnect_mnuViewAll ' 显示全部
            lblNum.Caption = "正在刷新..."
            GetWebConnectList CurrentPID, ListView, GetMenuCheckState(mnuWebConnect, FrmListView_mnuWebConnect_mnuViewAll)
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
            SetMenuCheckState mnuWebConnect, FrmListView_mnuWebConnect_mnuViewAll, Not GetMenuCheckState(mnuWebConnect, FrmListView_mnuWebConnect_mnuViewAll)
        Case FrmListView_mnuWebConnect_mnuLittleCopy ' 复制单格数据
            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
    End Select
End Sub

'[FrmListView.mnuWindow]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuWindow_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Dim hWnd As HWND = Cast(HWND, ValLng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 0), "0x", "&H")))
    Select Case wID
        Case FrmListView_mnuWindow_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            GetWindowList CurrentPID, ListView
            CommitListViewView ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuWindow_mnuShowWindow ' 显示窗口
            NtUserShowWindow(hWnd, SW_SHOW)
        Case FrmListView_mnuWindow_mnuHideWindow ' 隐藏窗口
            NtUserShowWindow(hWnd, SW_HIDE)
        Case FrmListView_mnuWindow_mnuMinWindow ' 最小化窗口
            NtUserShowWindow(hWnd, SW_SHOWMINIMIZED)
        Case FrmListView_mnuWindow_mnuMaxWindow ' 最大化窗口
            NtUserShowWindow(hWnd, SW_SHOWMAXIMIZED)
        Case FrmListView_mnuWindow_mnnuDestroyWindow ' 摧毁窗口
            NtUserDestroyWindow(hWnd)
            DeleteItemEx ListView, ListView.SelectedItem
        Case FrmListView_mnuWindow_mnuFreezeWindow ' 冻结窗口
            EnableWindow hWnd, False
        Case FrmListView_mnuWindow_mnuActiveWindow ' 激活窗口
            EnableWindow hWnd, True
            SwitchToThisWindow hWnd, True
        Case FrmListView_mnuWindow_mnuWindowTop ' 窗口置顶
            SetWindowPos hWnd, HWND_TOPMOST, 0, 0, 0, 0, SWP_NOMOVE Or SWP_NOSIZE
        Case FrmListView_mnuWindow_mnuWindowNoTop ' 取消置顶
            SetWindowPos hWnd, HWND_NOTOPMOST, 0, 0, 0, 0, SWP_NOMOVE Or SWP_NOSIZE
        Case FrmListView_mnuWindow_mnuLittleCopy ' 复制单格数据
            ' ==========================================
            ' 3. 复制到剪贴板
            ' ==========================================
            If (LastClickedItem >= 0 AndAlso LastClickedItem <= ListView.ItemCount - 1) AndAlso _
               (LastClickedSubItem >= 0 AndAlso LastClickedSubItem <= ListView.ColumnCount - 1) Then
                CopyDataToClipboard ListView.GetItemText(LastClickedItem, LastClickedSubItem)
            End If
   End Select
End Sub

'[FrmListView.mnuPrivilege]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuPrivilege_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Select Case wID
        Case FrmListView_mnuPrivilege_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            GetProcessPrivilegeList CurrentPID, ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuPrivilege_mnuEnablePrivilege ' 启用特权
            Dim strPrivilege As StringW = ListView.GetItemText(ListView.SelectedItem, 1)
            If AdjustPrivilege(CurrentPID, strPrivilege, True) Then
                AfxMsg "启用特权成功!"
                ListView.SetItemText ListView.SelectedItem, 2, "Enabled"
            Else
                AfxMsg "启用特权失败!"
            End If
        Case FrmListView_mnuPrivilege_mnuDisablePrivilege ' 禁用特权
            Dim strPrivilege As StringW = ListView.GetItemText(ListView.SelectedItem, 1)
            If AdjustPrivilege(CurrentPID, strPrivilege, False) Then
                AfxMsg "禁用特权成功!"
                ListView.SetItemText ListView.SelectedItem, 2, "Disabled"
            Else
                AfxMsg "禁用特权失败!"
            End If
   End Select
End Sub

'[FrmListView.mnuMsgHook]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuMsgHook_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Dim CurrentProcessName As StringW = GetNameByPath(GetProcessPath(CurrentPID))
    Select Case wID
        Case FrmListView_mnuMsgHook_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetMsgHookList GetMenuCheckState(mnuMsgHook, FrmListView_mnuMsgHook_mnuViewAll), CurrentPID, ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuMsgHook_mnuRemove ' 移除
            Dim hHook As HANDLE = Cast(HANDLE, ValULng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 0), "0x", "&H")))
            If UnhookWindowsHookEx(hHook) <> 0 Then
                AfxMsg "移除成功!"
                DeleteItemEx ListView, ListView.SelectedItem, True
            Else
                AfxMsg "移除失败!"
                MyLog.PrintWin32Error "FrmListView_mnuMsgHook_mnuRemove", "UnhookWindowsHookEx", "0x" & WHex(hHook)
            End If
        Case FrmListView_mnuMsgHook_mnuViewAll ' 显示全部
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            Dim bGlobal As Boolean = GetMenuCheckState(mnuMsgHook, FrmListView_mnuMsgHook_mnuViewAll)
            If bGlobal Then
                Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 消息钩子列表"
            Else
                Me.Caption = "消息钩子列表"
            End If
            GetMsgHookList Not bGlobal, CurrentPID, ListView
            SetMenuCheckState mnuMsgHook, FrmListView_mnuMsgHook_mnuViewAll, Not bGlobal
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuMsgHook_mnuEditMemory ' 查看/编辑内存
            If LastClickedSubItem <> 3 Then Exit Sub
            Dim Addr As ULONG64 = ValULng(FF_Replace(ListView.GetItemText(LastClickedItem, LastClickedSubItem), "0x", "&H"))
            Dim MemoryInfo As MemoryStruct Ptr = Allocate(SizeOf(MemoryStruct))
            MemoryInfo->Addr = Cast(PVOID, Addr)
            MemoryInfo->dwProcessId = Cast(HANDLE, Cast(ULONG64, CurrentPID))
            FrmMemoryEditor.Show,, Cast(Integer, MemoryInfo)
        Case FrmListView_mnuMsgHook_mnuCopyRowData ' 复制整行数据
            
    End Select
End Sub

'[FrmListView.mnuEventHook]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuEventHook_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Dim CurrentProcessName As StringW = GetNameByPath(GetProcessPath(CurrentInfo->ProcessId))
    Select Case wID
        Case FrmListView_mnuEventHook_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetEventHookList GetMenuCheckState(mnuEventHook, FrmListView_mnuEventHook_mnuViewAll), CurrentPID, ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuEventHook_mnuRemove ' 移除
            Dim hHook As HANDLE = Cast(HANDLE, ValULng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 0), "0x", "&H")))
            If UnhookWinEvent(hHook) <> 0 Then
                AfxMsg "移除成功!"
                DeleteItemEx ListView, ListView.SelectedItem, True
            Else
                AfxMsg "移除失败!"
                MyLog.PrintWin32Error "FrmListView_mnuEventHook_mnuRemove", "UnhookWinEvent", "0x" & WHex(hHook)
            End If
        Case FrmListView_mnuEventHook_mnuViewAll ' 显示全部
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            Dim bGlobal As Boolean = GetMenuCheckState(mnuEventHook, FrmListView_mnuEventHook_mnuViewAll)
            If bGlobal Then
                Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 事件钩子列表"
            Else
                Me.Caption = "事件钩子列表"
            End If
            GetEventHookList Not bGlobal, CurrentPID, ListView
            SetMenuCheckState mnuEventHook, FrmListView_mnuEventHook_mnuViewAll, Not bGlobal
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuEventHook_mnuEditMemory ' 查看/编辑内存
            If LastClickedSubItem <> 3 Then Exit Sub
            Dim Addr As ULONG64 = ValULng(FF_Replace(ListView.GetItemText(LastClickedItem, LastClickedSubItem), "0x", "&H"))
            Dim MemoryInfo As MemoryStruct Ptr = Allocate(SizeOf(MemoryStruct))
            MemoryInfo->Addr = Cast(PVOID, Addr)
            MemoryInfo->dwProcessId = Cast(HANDLE, Cast(ULONG64, CurrentPID))
            FrmMemoryEditor.Show,, Cast(Integer, MemoryInfo)
    End Select
End Sub

'[FrmListView.mnuHotKey]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuHotkey_WM_Command(hWndForm As hWnd,wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Dim CurrentProcessName As StringW = GetNameByPath(GetProcessPath(CurrentInfo->ProcessId))
    Select Case wID
        Case FrmListView_mnuHotkey_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetHotkeyList GetMenuCheckState(mnuHotkey, FrmListView_mnuHotkey_mnuViewAll), CurrentPID, ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuHotkey_mnuRemove ' 移除
            Dim id As Long = ValInt(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 1), "0x", "&H"))
            Dim hWnd As hWnd = Cast(HWND, ValULng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 3), "0x", "&H")))
            If UnregisterHotKey(hWnd, id) <> 0 Then
                AfxMsg "移除成功!"
                DeleteItemEx ListView, ListView.SelectedItem, True
            Else
                AfxMsg "移除失败!"
                MyLog.PrintWin32Error "FrmListView_mnuHotkey_mnuRemove", "UnregisterHotKey", "0x" & WHex(hWnd) & " 0x" & WHex(id)
            End If
        Case FrmListView_mnuHotkey_mnuViewAll ' 显示全部
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            Dim bGlobal As Boolean = GetMenuCheckState(mnuHotkey, FrmListView_mnuHotkey_mnuViewAll)
            If bGlobal Then
                Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 热键列表"
            Else
                Me.Caption = "热键列表"
            End If
            GetHotkeyList Not bGlobal, CurrentPID, ListView
            SetMenuCheckState mnuHotkey, FrmListView_mnuHotkey_mnuViewAll, Not bGlobal
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuHotkey_mnuEditMemory ' 查看/编辑内存
            Dim Addr As ULONG64 = ValULng(FF_Replace(ListView.GetItemText(LastClickedItem, LastClickedSubItem), "0x", "&H"))
            Dim MemoryInfo As MemoryStruct Ptr = Allocate(SizeOf(MemoryStruct))
            MemoryInfo->Addr = Cast(PVOID, Addr)
            MemoryInfo->dwProcessId = Cast(HANDLE, Cast(ULONG64, CurrentPID))
            FrmMemoryEditor.Show,, Cast(Integer, MemoryInfo)
    End Select
End Sub

'[FrmListView.mnuProcessTimer]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuProcessTimer_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Dim CurrentProcessName As StringW = GetNameByPath(GetProcessPath(CurrentInfo->ProcessId))
    Select Case wID
        Case FrmListView_mnuProcessTimer_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            GetProcessTimerList GetMenuCheckState(mnuProcessTimer, FrmListView_mnuProcessTimer_mnuViewAll), CurrentPID, ListView
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuProcessTimer_mnuRemove ' 移除
            Dim hWnd As hWnd = Cast(PVOID, ValULng(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 4), "0x", "&H")))
            Dim uIDEvent As ULong = ValUInt(FF_Replace(ListView.GetItemText(ListView.SelectedItem, 0), "0x", "&H"))
            If KillTimer(hWnd, uIDEvent) <> 0 Then
                AfxMsg "移除成功!"
                DeleteItemEx ListView, ListView.SelectedItem
            Else
                AfxMsg "移除失败!"
            End If
        Case FrmListView_mnuProcessTimer_mnuViewAll ' 显示全部
            lblNum.Caption = "正在刷新..."
            If Not IsDriverLoaded Then 
                If AfxMsg("驱动尚未加载,是否加载?",, MB_YESNO) = IDYES Then
                    If InitDriver Then AfxMsg "加载成功!" Else AfxMsg "加载失败!" : Return
                Else
                    Return
                End If
            End If
            Dim bGlobal As Boolean = GetMenuCheckState(mnuProcessTimer, FrmListView_mnuProcessTimer_mnuViewAll)
            If bGlobal Then
                Me.Caption = "[" & CurrentProcessName & " " & CurrentPID & "]" & " 进程定时器列表"
            Else
                Me.Caption = "进程定时器列表"
            End If
            GetProcessTimerList Not bGlobal, CurrentPID, ListView
            SetMenuCheckState mnuProcessTimer, FrmListView_mnuProcessTimer_mnuViewAll, Not bGlobal
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
    End Select
End Sub

'[FrmListView.mnuIOFunction]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuIOFunction_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Dim CurrentPID As DWORD = CurrentInfo->ProcessId
    Select Case wID
        Case FrmListView_mnuIOFunction_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            GetDriverIOFunctionList ListView, CurrentInfo->CurrentDriver.DriverPath, CurrentInfo->CurrentDriver.DriverObject
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
   End Select
End Sub

'[FrmListView.mnuMinifilterInstance]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuMinifilterInstance_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Select Case wID
        Case FrmListView_mnuMinifilterInstance_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            EnumMinifilterInstances ListView, @CurrentInfo->CurrentDriver.DriverName
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuMinifilterInstance_mnuDetachInstance ' 分离实例
            Dim FilterName As StringW = ListView.GetItemText(ListView.SelectedItem, 0)
            Dim VolumeName As StringW = ListView.GetItemText(ListView.SelectedItem, 1)
            Dim InstanceName As StringW = ListView.GetItemText(ListView.SelectedItem, 3)
            If DetachFilterInstance(FilterName.WstrPtr, VolumeName.WstrPtr, InstanceName.WstrPtr) Then
                AfxMsg "分离实例成功!"
                DeleteItemEx ListView, ListView.SelectedItem
            Else
                AfxMsg "分离实例失败!"
            End If
        Case FrmListView_mnuMinifilterInstance_mnuDetachAllInstances ' 分离所有实例
            Dim FilterName As StringW = ListView.GetItemText(ListView.SelectedItem, 0)
            If DetachAllFilterInstances(FilterName.WstrPtr) Then
                AfxMsg "分离所有实例成功!"
                DeleteAllItemsEx ListView
            Else
                AfxMsg "分离所有实例失败!"
                lblNum.Caption = "正在刷新..."
                EnumMinifilterInstances ListView, @CurrentInfo->CurrentDriver.DriverName
                lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
            End If
   End Select
End Sub

'[FrmListView.mnuFileStream]事件 : 点击了菜单项
'hWndForm 当前窗口的句柄(WIN系统用来识别窗口的一个编号，如果多开本窗口，必须 Me.hWndForm = hWndForm 后才可以执行后续操作本窗口的代码)
''           本控件为功能控件，就是无窗口，无显示，只有功能。如果多开本窗口，必须 Me.控件名.hWndForm = hWndForm 后才可以执行后续操作本控件的代码 
'wID      菜单项命令ID
Sub FrmListView_mnuFileStream_WM_Command(hWndForm As hWnd, wID As ULong)
    Me.hWndForm = hWndForm
    Me.ListView.hWndForm = hWndForm
    Dim CurrentInfo As CURRENT_INFORMATION Ptr = Cast(CURRENT_INFORMATION Ptr, Me.UserData(0))
    Select Case wID
        Case FrmListView_mnuFileStream_mnuRefresh ' 刷新
            lblNum.Caption = "正在刷新..."
            EnumerateFileStreams ListView, CurrentInfo->FilePath
            lblNum.Caption = "数量:" & WStr(ListView.ItemCount)
        Case FrmListView_mnuFileStream_mnuCreateStream ' 新建流
            Dim StreamName As StringW = AfxInputBox(,,, "提示", "请输入流的名称", "new")
            Dim StreamPath As StringW = CurrentInfo->FilePath & ":" & StreamName
            Dim hStream As HANDLE = CreateFileW(StreamPath.WstrPtr, GENERIC_WRITE, FILE_SHARE_WRITE, NULL, CREATE_NEW, FILE_ATTRIBUTE_NORMAL, NULL)
            If hStream <> INVALID_HANDLE_VALUE Then
                AfxMsg "创建流成功!"
                CloseHandle hStream
                AddItemColListEx ListView, 2,,, WStr(StreamName), WStr("0")
                CommitListViewView ListView
            Else
                AfxMsg "创建流失败!"
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateStream", "CreateFileW", "StreamPath = " & StreamPath
            End If
        Case FrmListView_mnuFileStream_mnuCreateStreamFromFile ' 从文件复制为流
            Dim StreamName As StringW = AfxInputBox(,,, "提示", "请输入流的名称", "new")
            Dim StreamPath As StringW = CurrentInfo->FilePath & ":" & StreamName
            Dim FilePath As StringW = AfxInputBox(,,, "提示", "请输入文件路径")
            Dim hStream As HANDLE = CreateFileW(StreamPath.WstrPtr, GENERIC_WRITE, FILE_SHARE_WRITE, NULL, CREATE_NEW, FILE_ATTRIBUTE_NORMAL, NULL)
            Dim hFile As HANDLE = CreateFileW(FilePath.WstrPtr, GENERIC_READ, FILE_SHARE_READ, NULL, OPEN_EXISTING, FILE_ATTRIBUTE_NORMAL, NULL)
            If hStream = INVALID_HANDLE_VALUE Then
                AfxMsg "创建流失败!"
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateStreamFromFile", "CreateFileW", "StreamPath = " & StreamPath
                Exit Sub
            End If
            If hFile = INVALID_HANDLE_VALUE Then
                CloseHandle hStream
                AfxMsg "打开文件失败!"
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateStreamFromFile", "CreateFileW", "FilePath = " & FilePath
                Exit Sub
            End If
            
            Dim bytData() As Byte, dwFileSizeLow As DWORD, dwFileSizeHigh As DWORD
            dwFileSizeLow = GetFileSize(hFile, @dwFileSizeHigh)
            Dim dwFileSize As ULongLong = dwFileSizeHigh * (2 ^ 32) + dwFileSizeLow
            ReDim bytData(dwFileSize - 1) As Byte
            Dim bytRead As DWORD = 0
            If ReadFile(hFile, @bytData(0), dwFileSize, @bytRead, NULL) = 0 OrElse bytRead <> dwFileSize Then
                CloseHandle hStream
                CloseHandle hFile
                AfxMsg "读取文件失败!"
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateStreamFromFile", "ReadFile", "FilePath = " & FilePath & " dwFileSize:" & dwFileSize & " bytRead:" & bytRead
                Exit Sub
            End If
            Dim bytWritten As DWORD = 0
            If WriteFile(hStream, @bytData(0), dwFileSize, @bytWritten, NULL) = 0 OrElse bytWritten <> dwFileSize Then
                AfxMsg "写入流失败!"
                CloseHandle hStream
                CloseHandle hFile
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateStreamFromFile", "WriteFile", "StreamPath:" & StreamPath & " FilePath = " & FilePath & " dwFileSize:" & dwFileSize & " bytWritten:" & bytWritten
                Exit Sub
            End If
            CloseHandle hStream
            CloseHandle hFile
            AfxMsg "从文件复制为流成功!"
            AddItemColListEx ListView, 2,,, WStr(StreamName), WStr(dwFileSize)
            CommitListViewView ListView
        Case FrmListView_mnuFileStream_mnuCreateFileFromStream ' 从流复制为文件
            Dim StreamPath As StringW = CurrentInfo->FilePath & ":" & ListView.GetItemText(ListView.SelectedItem, 0)
            Dim FilePath As StringW = AfxInputBox(,,, "提示", "请输入文件路径")
            Dim hStream As HANDLE = CreateFileW(StreamPath.WstrPtr, GENERIC_READ, FILE_SHARE_READ, NULL, OPEN_EXISTING, FILE_ATTRIBUTE_NORMAL, NULL)
            Dim hFile As HANDLE = CreateFileW(FilePath.WstrPtr, GENERIC_WRITE, FILE_SHARE_WRITE, NULL, CREATE_NEW, FILE_ATTRIBUTE_NORMAL, NULL)
            If hStream = INVALID_HANDLE_VALUE Then
                AfxMsg "打开流失败!"
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateFileFromStream", "CreateFileW", "StreamPath = " & StreamPath
                Exit Sub
            End If
            If hFile = INVALID_HANDLE_VALUE Then
                CloseHandle hStream
                AfxMsg "创建文件失败!"
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateFileFromStream", "CreateFileW", "FilePath = " & FilePath
                Exit Sub
            End If
            
            Dim bytData() As Byte, dwStreamSizeLow As DWORD, dwStreamSizeHigh As DWORD
            dwStreamSizeLow = GetFileSize(hStream, @dwStreamSizeHigh)
            Dim dwStreamSize As ULongLong = dwStreamSizeHigh * (2 ^ 32) + dwStreamSizeLow
            ReDim bytData(dwStreamSize - 1) As Byte
            Dim bytRead As DWORD = 0
            If ReadFile(hStream, @bytData(0), dwStreamSize, @bytRead, NULL) = 0 OrElse bytRead <> dwStreamSize Then
                CloseHandle hStream
                CloseHandle hFile
                AfxMsg "读取流失败!"
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateFileFromStream", "ReadFile", "FilePath = " & FilePath & " dwStreamSize:" & dwStreamSize & " bytRead:" & bytRead
                Exit Sub
            End If
            Dim bytWritten As DWORD = 0
            If WriteFile(hFile, @bytData(0), dwStreamSize, @bytWritten, NULL) = 0 OrElse bytWritten <> dwStreamSize Then
                AfxMsg "写入文件失败!"
                CloseHandle hStream
                CloseHandle hFile
                MyLog.PrintWin32Error "FrmListView_mnuFileStream_mnuCreateFileFromStream", "WriteFile", "StreamPath:" & StreamPath & " FilePath = " & FilePath & " dwStreamSize:" & dwStreamSize & " bytWritten:" & bytWritten
                Exit Sub
            End If
            CloseHandle hStream
            CloseHandle hFile
            AfxMsg "从流复制为文件成功!"
        Case FrmListView_mnuFileStream_mnuEditStream ' 修改流内容

        Case FrmListView_mnuFileStream_mnuDeleteStream ' 删除流

    End Select
End Sub



